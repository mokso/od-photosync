name: Build and Push Docker Image

on:
  push:
    branches:
      - main
      - python-rewrite
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # CalVer for main branch: YYYY.MM.DD
            type=raw,value={{date 'YYYY.MM.DD'}},enable={{is_default_branch}}
            # CalVer: YYYY.MM
            type=raw,value={{date 'YYYY.MM'}},enable={{is_default_branch}}
            # CalVer: YYYY
            type=raw,value={{date 'YYYY'}},enable={{is_default_branch}}
            # Branch name for development
            type=ref,event=branch
            # PR number
            type=ref,event=pr
            # Git tag support (semantic versioning)
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            # Latest tag on main branch
            type=raw,value=latest,enable={{is_default_branch}}
            # Commit SHA with branch prefix (only for main and PRs)
            type=sha,prefix={{branch}}-,enable=${{ github.ref == 'refs/heads/main' || github.event_name == 'pull_request' }}

      - name: Build and push Docker image
        id: build-push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

      - name: Image digest
        run: |
          echo "Image digest: ${{ steps.build-push.outputs.digest }}"
