# Volume mount configuration for Docker Compose
# Copy this file to .env and customize paths

# Path to photos on host (mounted inside container as /photos)
# For Proxmox LXC: This should be the bind mount path in LXC
# Example: /mnt/photos or /mnt/kuvat
PHOTOS_PATH=/mnt/kuvat

# Timezone for container
TZ=Europe/Helsinki

# Optional: Override schedule interval (minutes)
# SCHEDULE_INTERVAL=60
